# Translate Helper — 下载与使用

复制外语文本，自动翻译为简体中文（需联网）。

---

## 浏览器 / 网页上无法置顶？

| 运行方式 | 能否浮在 Chrome / Edge / 联想浏览器 上 |
| --- | --- |
| WSL / Linux 版 | **不能**（WSL 窗口与 Windows 浏览器是两套系统） |
| **Windows 原生版** | **可以**（使用 Windows API 全局置顶） |

### Windows 原生版（推荐，解决浏览器遮挡）

在 **Windows 资源管理器** 中进入项目目录，双击：

```
run-windows.bat
```

或打包独立 exe：

```cmd
build-app.bat
```

然后运行 `dist\TranslateHelper-0.1.0-win64\启动翻译助手.bat`

---

## 推荐：独立 App（解压即用，无需 Python）

### 下载

构建好的 Linux/WSL 版（约 24MB）：

```
dist/TranslateHelper-0.1.0-linux.tar.gz
```

或自行打包：

```bash
cd ~/translation_helper
bash build-app.sh
```

Windows 版（需在 Windows 上构建）：

```cmd
build-app.bat
```

若 Windows 上 `pip` 报 SSL/代理错误，在 WSL 中一键打包：

```bash
bash build-app-windows.sh
```

脚本会从 WSL 下载 Windows 离线 wheel，同步到 `C:\Users\lenovo\translate_helper_build` 并在 Windows 上完成 PyInstaller 打包。

生成 `dist\TranslateHelper-0.1.0-win64\` 和 `TranslateHelper-0.1.0-win64.zip`

### 使用（3 步）

**1. 解压**

```bash
tar -xzf TranslateHelper-0.1.0-linux.tar.gz
cd TranslateHelper-0.1.0-linux
```

**2. 配置 Token（首次）**

```bash
cp .env.example .env
nano .env    # 填写 HF_TOKEN
```

**3. 启动**

```bash
./启动翻译助手.sh
```

或在 Windows Terminal → WSL 里双击运行 `启动翻译助手.sh`。

> **WSL 用户**：必须在 Windows Terminal 的 WSL 窗口启动，悬浮窗才会显示在桌面。

---

## 备选：源码安装

```bash
git clone https://github.com/SteveRapeseed/Translate_helper.git
cd Translate_helper
bash install.sh
./run.sh
```

---

## 配置说明

`.env` 示例：

```dotenv
HF_TOKEN="你的_hf_token"
HF_BASE_URL="https://router.huggingface.co/v1"
HF_LLM_MODEL="moonshotai/Kimi-K2-Instruct-0905"
```

Token：https://huggingface.co/settings/tokens

---

## 使用说明

| 操作 | 效果 |
| --- | --- |
| 复制英/日/韩/越文本 | 自动翻译，展开面板 |
| 双击「译」小球 | 手动翻译剪贴板 |
| 右键小球 | 退出 |
| 钉住 | 保持面板展开 |

---

## 系统要求

- **独立 App**：Linux / WSLg，无需安装 Python
- **Windows App**：Windows 10+（需 build-app.bat 构建）
- 联网访问 HuggingFace API

---

## 常见问题

**看不到悬浮窗？** → 用 Windows Terminal 的 WSL 启动，不要用 IDE 内置终端。

**翻译失败？** → 检查 `.env` 里 `HF_TOKEN` 和网络。

**复制无反应？** → 换一段新文本；WSL 下从 Windows 程序复制会自动走剪贴板桥接。
